[package]
name = "rust-heat-equation"
version = "0.1.0"
edition = "2024"

[dependencies]
ndarray = { version = "0.17.2", default-features = false, features = ["std"] }
ndarray-linalg = { version = "0.18.1", default-features = false, features = ["blas"] }
